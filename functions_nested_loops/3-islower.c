#include "main.h"
/**
 * main - check the code.
 *
 * Return: Always 0.
 */
int _islower(int c)
{
	int o = 0;
	int n = 1;

	while (n <= 1)
	{
	for (o = '0' ; o <= '1' ; n++)
	{
	putchar(o);
	}
	putchar('\n');
	n++;
	}
	return (0);
}
