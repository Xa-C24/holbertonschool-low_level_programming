#include "main.h"
/**
 * _isdigit: Check if a caracter it's a digit
 * @c: number checked
 *
 *Return : 1 if truc, 0 if fasle
 */
int _isdigit(int c)
{
	if (c >= '0' && c <= '9')
	{
	return (1);
	}
	else
	return (0);
}
