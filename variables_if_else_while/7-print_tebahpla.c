#include <stdio.h>

int main(void)
/**
 * main - Prints the last digit of a randomly generated number
 * Descrption:Prints the numbers 1 à 9
 *
 * Return: Always 0.
 */
{
	char low;
	
	for (low = 'z' ; low >= 'a'; low--)
	putchar(low);
	putchar('\n');
return (0);
}
